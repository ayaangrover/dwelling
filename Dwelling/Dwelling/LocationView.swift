import SwiftUI

struct LocationView: View {
    var body: some View {
        Text("This is the location view")
            .font(.largeTitle)
            .padding()
    }
}

struct LocationView_Previews: PreviewProvider {
    static var previews: some View {
        LocationView()
    }
}
